spring:
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:tcp://localhost:9999/~/.batch/something-batch
      username: sa
      password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

batch:
  name: 'SOMETHING'
  target:
    path: 'C:\workspace\batch\cbms\fixed-length-file'
